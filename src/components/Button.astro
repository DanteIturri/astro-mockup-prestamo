---
import { Icon } from 'astro-icon/components';
interface Props {
  text: string;
  icon?: string;
  color?: string;
}
 const { text, icon, color } = Astro.props;
---

<button>{
  icon && <Icon name={icon} width={10} height={10} color={color} />
  }
  {text}
</button>